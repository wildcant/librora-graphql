query SearchBooks($input: SearchBooksInput!) {
  searchBooks(input: $input) {
    nodes {
      id
      title
      cover
      date
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
    }
    totalCount
  }
}
