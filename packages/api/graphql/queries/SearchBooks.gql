query SearchBooks($input: SearchBooksInput!) {
  searchBooks(input: $input) {
    nodes {
      id
      title
      cover
      date
      slug
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
    }
    totalCount
  }
}
