query Book($id: String!) {
  book(id: $id) {
    id
    title
    subtitle
    description
    author {
      id
      name
    }

    owner {
      id
      firstName
      lastName
      createdAt
    }
  }
}
